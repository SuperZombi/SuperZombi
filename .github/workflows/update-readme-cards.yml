name: Update README cards

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        repo:
          - Picture-in-Picture-for-Youtube
          - HDrezka-Helper
          - telegram-downloader
          - genshin-resin
          - Google-Meet-Helper
          - Instagram-Helper

    steps:
      # - uses: actions/checkout@v6

      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 22

      # GitHub stats
      # - name: Stats (light)
      #   uses: readme-tools/github-readme-stats-action@v1
      #   with:
      #     card: stats
      #     options: username=${{ github.repository_owner }}&show_icons=true&border_radius=15
      #     path: stats/stats-light.svg
      #     token: ${{ secrets.GITHUB_TOKEN }}

      # - name: Stats (dark)
      #   uses: readme-tools/github-readme-stats-action@v1
      #   with:
      #     card: stats
      #     options: username=${{ github.repository_owner }}&show_icons=true&border_radius=15&theme=dark
      #     path: stats/stats-dark.svg
      #     token: ${{ secrets.GITHUB_TOKEN }}

      # Top languages
      # - name: Langs (light)
      #   uses: readme-tools/github-readme-stats-action@v1
      #   with:
      #     card: top-langs
      #     options: username=${{ github.repository_owner }}&border_radius=15
      #     path: stats/langs-light.svg
      #     token: ${{ secrets.GITHUB_TOKEN }}

      # - name: Langs (dark)
      #   uses: readme-tools/github-readme-stats-action@v1
      #   with:
      #     card: top-langs
      #     options: username=${{ github.repository_owner }}&border_radius=15&theme=dark
      #     path: stats/langs-dark.svg
      #     token: ${{ secrets.GITHUB_TOKEN }}

      # ---------- Pin cards ----------
      - name: Pin (light) — ${{ matrix.repo }}
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=${{ github.repository_owner }}&repo=${{ matrix.repo }}&border_radius=15
          path: stats/pins/${{ matrix.repo }}-light.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Pin (dark) — ${{ matrix.repo }}
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=${{ github.repository_owner }}&repo=${{ matrix.repo }}&border_radius=15&theme=dark
          path: stats/pins/${{ matrix.repo }}-dark.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      # Commit
      - name: Upload SVGs
        uses: actions/upload-artifact@v4
        with:
          name: pins-${{ matrix.repo }}
          path: stats/pins/
      # - name: Commit SVGs
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      #     git add stats/pins/*.svg
      #     git commit -m "Update README cards" || exit 0
      #     git push
